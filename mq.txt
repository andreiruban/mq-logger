CRTMQM QM01
STRMQM QM01
RUNMQSC QM01

DEFINE QLOCAL(INPUT1)
DEFINE QLOCAL(INPUT2)
DEFINE QLOCAL(OUTPUT1)

DEFINE LISTENER(QM01.LISTENER) TRPTYPE (TCP) PORT(1414)
START LISTENER(QM01.LISTENER)

DEFINE CHANNEL(CHAN1) CHLTYPE(SVRCONN) TRPTYPE(TCP)

SET CHLAUTH(CHAN1) TYPE(ADDRESSMAP) ADDRESS('IP addressв') MCAUSER('userid')
ALTER QMGR CHLAUTH(DISABLED)

END
